'use client';

export default function Navigation() {
  return (
    <nav
      className="fixed bottom-10 left-[calc(50vw-45vw)] flex items-center gap-6 z-30 max-sm:bottom-8 max-sm:left-8 max-sm:gap-4"
      aria-label="Main navigation"
    >
      {/* <a
        href="#about"
        className="group relative text-gray-400 no-underline font-light text-xs tracking-[0.25em] uppercase transition-colors duration-300"
      >
        <span className="inline-block transition-transform duration-300 ease-out group-hover:-translate-y-1 group-hover:text-gray-100">
          about
        </span>
      </a> */}
    </nav>
  );
}
